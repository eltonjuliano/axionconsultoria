[project]
name = "axion-consultoria"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.6",
    "gunicorn>=26.0.0",
    "psycopg2-binary>=2.9.12",
]
